/** @type {import('next').NextConfig} */
const nextConfig = {
  // Viktigt: ingen `output: 'export'` här!
  reactStrictMode: true,
  // valfritt: experimental: { forceSwcTransforms: true }
};
export default nextConfig;
